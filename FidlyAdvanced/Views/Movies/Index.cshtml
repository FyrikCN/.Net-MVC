@model IEnumerable<FidlyAdvanced.Models.Movie>
@{
    ViewBag.Title = "Movies";
}

<h2>Movies</h2>

@if (Model.Count() == 0)
{
    <p>There is no movie yet.</p>
}
else
{
    <ul>
        @foreach(var movie in Model)
        {
            <li>@movie.Name</li>
        }
    </ul>
}